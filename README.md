# Environment Variables

This app currently does not require any environment variables to run locally.

## Optional
- `PORT` - overrides the backend port (defaults to `4000`)

## Local run
1. Install dependencies in `backend/` and `frontend/`
2. Start the backend: `PORT=4000 node backend/index.js`
3. Start the frontend: `npm start` from `frontend/`
