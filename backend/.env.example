# Copy this file to .env and replace only the placeholder values.
# Never commit backend/.env or paste credentials into source control.

AWS_REGION=us-east-2
AWS_BEARER_TOKEN_BEDROCK=replace-with-a-new-bedrock-token
BEDROCK_MODEL_ID=amazon.nova-pro-v1:0
PORT=4000